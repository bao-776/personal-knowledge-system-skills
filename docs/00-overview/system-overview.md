# 系统总览

系统将个人知识工作拆成三个数据层和一组按需能力。

```text
Raw 原始证据 → Wiki 可维护理解 → Outputs 场景成品
                        ↓
          成长、项目、观察、复盘与实践
```

`knowledge-vault` 负责识别当前阶段。其他 Skill 各自处理一种明确工作，避免在一次调用中加载全部规则。方法层保持 Markdown 通用；`obsidian-vault` 提供 Obsidian 增强。

