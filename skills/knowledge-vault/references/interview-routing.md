# Interview routing

Inspect first. Ask only decisions that cannot be derived from existing files. Ask one high-impact question at a time.

For a new system: purpose → success evidence → inputs → retrieval → outputs → privacy → maintenance capacity.
For an existing system: pain point → current evidence → desired behavior → compatibility → migration tolerance.
Stop when the next action is decision-complete; do not interview for optional future modules.
