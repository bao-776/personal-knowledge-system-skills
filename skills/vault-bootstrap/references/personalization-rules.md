# Personalization rules

Configure language, naming, metadata, statuses, review cadence, entry points, modules, and disclosure. Distinguish defaults from requirements.

Do not infer categories from age, job, school, family, or personality. Use the user's actual retrieval and output needs. Leave unused modules absent.
