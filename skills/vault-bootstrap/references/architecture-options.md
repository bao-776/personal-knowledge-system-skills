# Architecture options

Default to Raw, Wiki, and Outputs. Add Inbox for unclassified input; Projects, Learning, Reviews, Self, and System only when used.

For an existing vault, map current folders to responsibilities before proposing changes. Prefer adapters and indexes over bulk moves. Keep Obsidian-specific features optional.
