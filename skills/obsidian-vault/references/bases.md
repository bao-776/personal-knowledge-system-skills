# Bases

Use native Bases for repeated fields, filters, sorting, and views. Define one clear source folder or property filter. Each view should answer a user question. Keep data in notes; the Base is a view, not the source of truth. Provide a Markdown fallback.
