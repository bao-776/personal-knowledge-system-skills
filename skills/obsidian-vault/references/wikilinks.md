# Wikilinks

Use `[[Note]]` or `[[Note|Label]]` for direct relationships. Check that targets resolve. Backlinks provide reverse discovery; do not manually duplicate every reverse link. Preserve aliases during renames and avoid links based only on possible relevance.
