# Atomicity criteria

Create one note when it has one durable question or conclusion, can be named precisely, retains necessary context, and can evolve independently.

Keep material together when parts are meaningful only as a sequence, share one lifecycle, or splitting would duplicate context. Atomicity is about independent maintenance, not short length.
